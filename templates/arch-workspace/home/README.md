<!-- NH_SETUP_VERSION: 2.0 default -->
<!-- Profile: Arch ARM64 v2.0 default; Kali ARM64 v2.0 default -->

# Arch User Home

This home directory is for shell configuration and small personal files.

Main work belongs in `/workspace`.

Useful shortcuts:

```sh
cd ~/work
cd ~/projects
cd ~/src
cd ~/builds
cd ~/labs
cd ~/notes
```

