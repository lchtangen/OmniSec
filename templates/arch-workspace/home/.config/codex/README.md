# Codex Notes

Codex is installed as the `archlinux` user.

Start from a project directory:

```sh
cd /workspace/01-projects/active
codex
```

Keep secrets out of prompts and project files.
<!-- NH_SETUP_VERSION: 2.0 default -->
<!-- Profile: Arch ARM64 v2.0 default; Kali ARM64 v2.0 default -->

