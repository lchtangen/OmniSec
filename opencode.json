{
  "$schema": "https://opencode.ai/config.json",
  "model": "kilo/kilo-auto/free",
  "instructions": [
    "AGENTS.md",
    "OPENCODE.md"
  ],
  "command": {
    "lint": {
      "template": "Run OmniSec validation: shell syntax check, C syntax check, and JSON validation.",
      "description": "Run shell, C, and JSON linting",
      "agent": "build"
    },
    "test": {
      "template": "Run OmniSec test suite: BATS shell tests and C unit tests.",
      "description": "Run BATS + C tests",
      "agent": "build"
    },
    "validate": {
      "template": "Run full OmniSec quality gate: lint, test, then validate.sh.",
      "description": "Full quality gate (lint + test + validate)",
      "agent": "build"
    },
    "stage": {
      "template": "Regenerate payload/ from src/. Copies device binaries, configs, and AI agent. Run after modifying any source payload files.",
      "description": "Stage payload from src/",
      "agent": "build"
    },
    "ai-context": {
      "template": "Generate a repo context snapshot for AI agents by running make ai-context.",
      "description": "Generate repo context snapshot",
      "agent": "explore"
    },
    "status": {
      "template": "Show overall OmniSec device status via nhctl status.",
      "description": "Show device status via nhctl",
      "agent": "build"
    }
  },
  "permission": {
    "read": "allow",
    "glob": "allow",
    "grep": "allow",
    "list": "allow",
    "edit": "allow",
    "write": "allow",
    "bash": "allow",
    "skill": "allow",
    "webfetch": "allow",
    "websearch": "allow",
    "external_directory": {
      "/tmp/opencode/*": "allow",
      "/home/tangen/.local/share/opencode/*": "allow"
    }
  }
}
