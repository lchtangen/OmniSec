# NH_SETUP_VERSION: 2.0 default
# Profile: Arch ARM64 v2.0 default; Kali ARM64 v2.0 default

# Dependencies
node_modules/

# Build artifacts
build/
dist/

# Generated audit/output logs
audits/
*.log
*.tmp
*.bak
*.nhbak

# Large local rootfs artifacts. Keep locally, do not push to GitHub.
ArchLinuxARM-aarch64-latest.tar.gz
*.tar
*.tar.gz
*.tgz
*.xz
*.img

# Partition backups
backups/partitions/

# Local/editor/private state
.claude/settings.local.json
.claude/settings.*.json
.claude/projects/
.claude/todos/
.claude/cache/
.codex/
.aider*
# Track the base project Aider config, ignore user overrides
!.aider.conf.yml
.aider.conf.local.yml
.cursorrules.local
.clinerules.local
.cursor/
.windsurf/
.continue/
# Track the base project Continue config, ignore user overrides
!.continue/config.json
.continue/config.local.json
.gemini/
.codeium/
.cody/
.sourcegraph/
.copilot/
.opencode/
.agents.local/
.github/prompts/*.local.prompt.md
.github/prompts/*.private.prompt.md
.github/copilot-instructions.local.md
.env
.env.*
*.key
*.pem
id_ed25519*
known_hosts

# Duplicate/restructuring directories
src/
deploy/
workspace/

# Binary artifacts
*.deb
*.zip
*.apk

# Local binary assets (rootfs, firmware, APKs)
assets/

# Build output (created by make dist, make build-c)
dist/

# Untracked local development directories
src/
deploy/

# Nested repos (separate projects, not submodules)
kernel-build-arch-package/

# Local device backup data
device-backup/

# Buildozer Python-for-Android nested repo
platform-legacy/mobile/kivy_app/.buildozer/

# OS/editor noise
.DS_Store
Thumbs.db
*~
*.swp
OmniSec-dev/
