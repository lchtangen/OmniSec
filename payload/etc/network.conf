# NetHunter Matrix v3.0 - Network Configuration'

# ADB Settings'
ADB_SERIAL="${ADB_SERIAL:-10.0.0.113:52104}"'
ADB_PORT="${ADB_PORT:-52104}"'

# SSH Settings'
ARCH_SSH_PORT=2222'
KALI_SSH_PORT=22'
TERMUX_SSH_PORT=8022'

# Wireless Interface'
WIFI_IFACE="${WIFI_IFACE:-wlan0}"'
BT_IFACE="${BT_IFACE:-hci0}"'

# Mesh Networking'
RETICULUM_ENABLED=false'
LXMF_ENABLED=false'
YGGDRASIL_ENABLED=false'
EOF'
echo "Created network.conf!"